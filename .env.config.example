# ─────────────────────────────────────────────────────────────
# Helix Automation Framework Example Configuration
# Copy this file to:  .env.config
# Do NOT commit real credentials.
# ─────────────────────────────────────────────────────────────

# Environment name (dev, qa, stage, prod, etc.)
HELIX_ENV=dev

# Base URL for the Web UI
HELIX_BASE_URL=https://www.passthenote.com

# Base URL for API testing (RestAssured, etc.)
HELIX_API_BASE_URL=https://www.passthenote.com/api/v1

# Credentials for local testing
# Override in CI using environment variables or secrets.
HELIX_USERNAME=your.email@example.com
HELIX_PASSWORD=YourStrongPassword

# Browser: chrome | firefox | edge
HELIX_BROWSER=chrome

# Test run mode: FULL | SMOKE | SANITY
HELIX_RUN_TYPE=FULL

# Default explicit wait timeout (seconds)
HELIX_TIMEOUT=10
