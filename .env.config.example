# Local example config — copy to .env.config (DO NOT COMMIT credentials)
# Web UI base URL (host only)
BASE_URL=https://www.passthenote.com

# API gateway base (used by RestAssured)
API_BASE_URL=https://www.passthenote.com/api/v1

# Credentials for local/dev testing — set in CI secrets instead
USERNAME=your.email@example.com
PASSWORD=YourStrongPassword

# Preferences
BROWSER=chrome
RUN_TYPE=FULL
TIMEOUT=10
